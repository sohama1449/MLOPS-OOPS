1. clas, object
2. class has data/ att, and has some functionalities

attributes are called automatically, methods are called manually

** constructor methos is called quickly after obj created means --> " bante hi call ho jata hai"

** why constructor is special:
--> bcz the attributes and data in constructor are called automatically as soon as we created obj

** what is  need of it to call automatically :
-> we defined attributes or data in constructor which we cant give the user to define manually so it run after callinga automatically.

** why py is obj oriented programming:
--> any random data type in py is class 
--> everythin in py is obj of an any class

** adv of oops :
--> we can create our own datatype
    a+b = a+b but in py if we mention print(x+y) = xy, so in py still there is no any alebra datatype but we can make it using oops
    code resuable
    debugging
    easy to colab

diff btn func & methos :
--> In Python, a function is an independent, reusable block of code that performs a task. A method is also a function, but it is defined inside a class and is associated with an object or class.
    Functions are generally called directly by their name, while methods are called using an object or class with dot notation.
    An instance method usually receives self, which refers to the current object

** id :
--> id denotes the location of memory on which our data is saved


**Encapsulaion:
--> for security purpose if we want to hide any attributes or methods from anyone we just keep __ before method or attribute
    We cant protect 100percent in python --> so we can use another language

** getter and setter :
--> to get and set hidden att (for def <any name> (self):)  function name can any

**static method:
--> @staticmethod : if we not mention self in argument is allow we can direct giveparameteres
     using static method deirectly from class rather than obj (static method has no need of object)

** inheritance:
--> child can inherit att and methods from parent or base class
    reusable code, paent has no access of child class
    what inerites : constructor, non-private att, non-private methods 
    Method overloading means using the same method name with different numbers or types of parameters.


** super     :
--> fetch parents constructor or methods 
    only used inside "child class", not with object


** inheritance :
1. single
2. multilevel
3. hierarchial
4. multiple
5. hybrid